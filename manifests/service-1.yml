apiVersion: v1
kind: Service
metadata:
    name: "azehmwcluster-1f98"
    labels:
        app: "azehmwcluster-1f98"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "azehmwcluster-1f98"